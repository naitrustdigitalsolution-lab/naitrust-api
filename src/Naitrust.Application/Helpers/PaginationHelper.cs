namespace Naitrust.Application.Helpers;

public static class PaginationHelper
{
    public static (int Offset, int Page, int TotalPages) Calculate(int totalCount, int page, int pageSize) =>
        throw new NotImplementedException();
}
